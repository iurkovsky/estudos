{   
    {
         {
             { 
                 { 
                    var sera = 'sera???' 
                 }
             }
         }
        }
    }
    console.log(sera)

        function teste() {
            var local = 123
        }
        teste()
        console.log(local)

        // quando o var esta dentro de uma função, ele nao podera ser chamado em qualquer instancia do código
        // cuidar para nunca criar um var que seja global, pois voce irá lembrar dela, 
        //mas se passar o codigo para outra pessoa, ela podera reescrever e acabar dando problema no código inteiro